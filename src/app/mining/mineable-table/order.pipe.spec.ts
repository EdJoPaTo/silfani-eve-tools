/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { OrderPipe } from './order.pipe';

describe('Pipe: Order', () => {
  it('create an instance', () => {
    let pipe = new OrderPipe();
    expect(pipe).toBeTruthy();
  });
});
